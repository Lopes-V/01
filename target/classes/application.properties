# Configura��es do banco de dados
spring.datasource.url=jdbc:mysql://localhost:3356/bancoteste01?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC


# Configura��es de autentica��o do banco de dados
spring.datasource.username=root

# Configura��es de autentica��o do banco de dados
spring.datasource.password=1212

# Configura��es do JPA e Hibernate
spring.jpa.hibernate.ddl-auto=update

# Configura��es de exibi��o de SQL no console
spring.jpa.show-sql=true


# Configura��es do driver JDBC para MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configura��es de pool de conex�es HikariCP
spring.datasource.hikari.initialization-fail-timeout=30000

# Configura��es de dialeto do Hibernate para MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Configura��es de dialeto do Hibernate para MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect